<div class="flip flip-vertical">
  <div class="front">
    <img src={{imageUrl}} alt="Jungle-Cruise" />
  </div>
  <div class="back">
    <h1>{{title}}</h1>
    <h2>Genre: {{genre}}</h2>
    <p>Description: {{description}}</p>
    <a href="/movies/{{_id}}" class="details">Details</a>
    <a href="/movies/{{_id}}/attach" class="details">Attach</a>
  </div>
</div>
      